<Window x:Class="WpfApp1.User"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="User" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button x:Name="Exit" Content="Выйти" Click="Exit_Click" VerticalAlignment="Top"/>
        <Button x:Name="EditTest"  Content="Редактировать тест" Click="EditButton_Click" Grid.Column="1" VerticalAlignment="Top"/>
        <Button x:Name="Test" Content="Пройти тест" Grid.Column="2" Click="Button_Click" VerticalAlignment="Top"/>
        <Frame x:Name="PageFrame" Grid.Row="0" Grid.ColumnSpan="6" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>